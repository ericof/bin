# .proﬁle # ~/.proﬁle: executed by the command 
# interpreter for login shells. 
# This ﬁle is not read by bash(1), if ~/.bash_proﬁle # or ~/.bash_login exists. 
# if running bash 
if [ -n "$BASH_VERSION" ]; then 
    # include .bashrc if it exists 
    if [ -f ~/.bashrc ]; then 
        .~/.bashrc 
    ﬁ 
ﬁ
